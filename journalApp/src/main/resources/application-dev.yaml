spring:
  application:
    name: journalApp
  data:
    mongodb:
      uri:
      database: journaldb
      auto-index-creation: true
server:
  port: 8080
logging:
  level:
    net:
      springproject:
        journalApp: DEBUG
#logging:
#  level:
#    root: off